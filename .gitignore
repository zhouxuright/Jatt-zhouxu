# ============================================================================
# 法律智能辅助系统 — .gitignore
# ============================================================================
# 注意：本仓库推送到的是 **公开** GitHub 仓库（zhouxuright/Jatt-zhouxu）。
# 因此凭证、个人文件、客户数据、生成产物一律不入库。
# ============================================================================

# ---------------------------------------------------------------------------
# 凭证 / 环境变量（绝不入库）
# ---------------------------------------------------------------------------
.env
.env.*
!.env.example
backend/.env
backend/.env.*
!backend/.env.example

# ---------------------------------------------------------------------------
# 个人文件 / 机密 / 调试产物
# ---------------------------------------------------------------------------
# 简历（含姓名、手机号、邮箱、籍贯）
/_resume_text.txt
/*.pdf
# 真实合同与接口探查产物
/_probe_cr/
backend/scripts/_probe_token.txt

# ---------------------------------------------------------------------------
# 数据（约 18GB：16GB 合成数据 + 导入检查点 + 原始数据集）
# 这些是运行时输入，不是源代码；代码里以 data/... 相对路径引用即可
# ---------------------------------------------------------------------------
backend/data/
backend/reports/
backend/uploads/
backend/chroma_data/
# 爬取产物（含爬虫落地的案例 JSON）
backend/crawler_data/
backend/crawler_data_playwright/
backend/crawler_data_flk/
backend/crawler_data_public/

# 数据库文件
*.db
*.sqlite3

# ---------------------------------------------------------------------------
# Python
# ---------------------------------------------------------------------------
# 注意 venv_gpu（5GB，GPU 环境）—— 不能只写 venv/
backend/venv/
backend/venv_gpu/
venv/
venv*/
__pycache__/
*.pyc
*.pyo
*.egg-info/
.pytest_cache/
.mypy_cache/
.ruff_cache/

# ---------------------------------------------------------------------------
# Node
# ---------------------------------------------------------------------------
frontend/node_modules/
frontend/dist/
node_modules/

# ---------------------------------------------------------------------------
# 工具状态目录（本地 AI 助手 / IDE 状态，含本地路径）
# ---------------------------------------------------------------------------
.mimosa/
.workbuddy/
.agent-browser-home/
.claude/

# ---------------------------------------------------------------------------
# IDE / OS
# ---------------------------------------------------------------------------
.vscode/
.idea/
Thumbs.db
.DS_Store

# ---------------------------------------------------------------------------
# 模型缓存
# ---------------------------------------------------------------------------
.huggingface_cache/
